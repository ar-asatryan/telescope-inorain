{
  "mcpServers": {
    "postgres": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-postgres",
        "postgresql://admin-db:db-password@localhost:5432/telescope-db"
      ]
    }
  }
}
